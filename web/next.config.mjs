/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router is enabled by default in Next 14 when you use /app,
  // so we don't need experimental.appDir anymore.
};

export default nextConfig;
